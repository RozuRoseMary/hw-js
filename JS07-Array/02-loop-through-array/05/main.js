// **ให้ใช้** forEach และ console.log ให้ได้ผลลัพธ์แบบใน comment ด้านล่าง

const names = ["Mike", "Sid", "Jack", "Bill"];
// 1. Mike
// 2. Sid
// 3. Jack
// 4. Bill
names.forEach((el, i) => console.log(`${i + 1}. ${el}`));
